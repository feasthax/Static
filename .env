# Server
PORT=3000
BASE_URL=http://localhost:3000

# Mongo
MONGODB_URI=mongodb+srv://ricuspuser:10wkfgPPMoCt6zkU@api.hetg9.mongodb.net/?appName=api/keysite

# Secret used to HMAC-sign keys. Generate a long random string.
KEY_SECRET=change_me_to_a_long_random_string

# Key lifetime in hours
KEY_TTL_HOURS=24

# --- Ad provider (fill in once chosen) ---
# The URL you send users to (your linkvert/ad destination)
AD_REDIRECT_URL=
# Shared secret the provider uses to sign server-to-server postbacks.
# Leave blank to use return-token-only mode (less secure).
AD_POSTBACK_SECRET=
